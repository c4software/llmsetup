# =============================================================================
# llama-swap : routeur de modèles gufo (runtime-gufo/docker-compose.yml)
#
# gufo démarre toujours derrière llama-swap. Le client choisit le modèle par le
# champ « model », comme avec le routeur llama-server du service : llama-swap
# lance le serveur gufo de ce modèle (« gufo serve llm|tts|asr|image ») et
# arrête ce qui doit l'être. Trois groupes (routing, en bas du fichier) :
#   - « gros » : les LLM et Qwen-Image, un seul à la fois ; aucune paire ne
#     tient en mémoire (Flash-Next ~100 Gio en 2 sessions, DeepSeek ~93 Gio,
#     27B ~45 Gio, Qwen-Image 33 Go de poids BF16, 124 Gio en tout) ;
#   - « voix » : les trois variantes de Qwen3-TTS, une à la fois, à côté du
#     gros modèle, jamais déchargées par lui, déchargées après 60 s sans
#     requête (gufo-org/gufo#272) ;
#   - « transcription » : Qwen3-ASR, à côté de tout, jamais déchargé par un
#     autre modèle, déchargé après 60 s sans requête (même raison).
# Bascule mesurée le 25/09/2026 : vers le 27B 64,6 s, vers Flash-Next 31,4 à
# 32,9 s, vers DeepSeek 70,4 s.
# Clients agentiques : faire pointer TOUS leurs rôles (principal et tâches
# annexes, le « Haiku » de Claude Code) sur le même modèle, sinon chaque
# requête annexe déclenche une bascule.
#
# Fichier VERSIONNÉ, SEULE description des lignes de commande de gufo, sans
# valeur machine : chemins, sessions et modèle préchargé viennent de
# l'environnement du conteneur (${env.VAR}, posé par le compose depuis le .env
# que génère ./setup-llm.sh --gufo). Lu au démarrage : un changement demande
# ./setup-llm.sh --gufo, qui recrée le conteneur. Validation :
# llama-swap -config gufo-llama-swap.yaml -validate (avec les variables posées).
#
# Réglages communs (macro gufo) et leur origine, détaillés dans docs/GUFO.md
# (« Paramètres de gufo et leur origine ») :
#   --context 262144      contexte natif, celui du service (défaut gufo 4096)
#   --sessions N          2 par défaut : la seconde évite qu'une requête annexe
#                         du client (titre, résumé) évince la conversation
#                         principale de la RAM (+7 Gio)
#   --max-tokens 32768    défaut gufo 128 : un client qui n'envoie pas
#                         max_tokens serait coupé
#   --cache-disk …        seul chemin qui reprend le préfixe commun de deux
#                         conversations. Rétention au défaut de gufo (8 Gio
#                         depuis gufo-org/gufo#279, d9a84f1 ; 16 Gio
#                         explicites avant le 26/09/2026). Staging 8 Gio,
#                         non préalloué depuis #279 : le défaut automatique
#                         (au plus 1 Gio) refusait le 26/09/2026 les points
#                         de reprise de nos sessions Flash-Next (1,39 à
#                         1,50 Go, contextes de 40 à 60k tokens) et
#                         refuserait ceux du 27B dès ~8k tokens (165 Mo +
#                         0,1 Mo par token, le prompt système de Claude Code
#                         seul ~2,1 Go). Refus journalisés depuis #279
#                         (reason=staging_capacity). Changer la
#                         configuration rend ce cache inutilisable. Partagé
#                         entre les modèles (l'identité du cache inclut le
#                         modèle).
# =============================================================================

# Flash-Next ou DeepSeek à froid : ~80 s ; marge pour un disque chargé.
healthCheckTimeout: 300

# Jamais de messages « chargement en cours » dans le champ de raisonnement :
# un client agentique les prendrait pour du contenu.
sendLoadingState: false

# Journaux de llama-swap ET de gufo sur la sortie du conteneur :
# ./setup-llm.sh --gufo-logs y lit les lignes event=completed de gufo.
logToStdout: "both"

# Pas de déchargement sur inactivité : le dernier modèle reste servi, comme
# le routeur du service avec son préchargement. Exception : les voix (ttl
# propre, voir Qwen3-TTS plus bas) et la transcription.
globalTTL: 0

macros:
  # Partie commune de la ligne de commande de gufo, qui écoute en local sur le
  # port que llama-swap lui attribue.
  "gufo": >-
    gufo serve llm --host 127.0.0.1 --port ${PORT}
    --context 262144 --sessions ${env.GUFO_SESSIONS} --max-tokens 32768
    --cache-disk ${env.GUFO_DATA}/cache --cache-disk-staging-bytes 8589934592
  # Échantillonnage officiel Qwen, profil instruct (sections nothink du
  # service) ; défauts serveur, un client peut les surcharger par requête.
  "qwen": >-
    --temperature 0.7 --top-k 20 --top-p 0.8 --min-p 0.0
    --presence-penalty 1.5 --think off
  # Échantillonnage de la section deepseek-v4-flash du service. Raisonnement
  # laissé au gabarit du modèle (gufo n'a pas de budget de raisonnement).
  "deepseek": >-
    --temperature 1.0 --top-k 40 --top-p 0.95 --min-p 0.0

# Le nom de modèle EST le --served-model-name de gufo : llama-swap transmet la
# requête avec ce nom, gufo répond 404 model_not_found sinon. stop (OpenAI)
# et stop_sequences (Anthropic) passent tels quels : gufo les gère depuis le
# 25/09/2026 (gufo-org/gufo#260, 9854ca5) ; ils étaient retirés ici avant.
# capabilities.context : llama-swap n'interroge pas gufo sur son contexte, et
# sans lui /v1/models n'en publie aucun ; le proxy (qui lit meta.n_ctx) n'en
# transmet alors pas, et l'extension pi/omp retombe sur 128K. Doit rester égal
# au --context de la macro gufo.
models:
  # Qwen3.8-27B, fichiers du parc (./setup-llm.sh --setup) : cible UD-Q4_K_XL,
  # drafter DFlash 2 Q8_0 (adaptatif, 7 tokens max, défauts gufo).
  "qwen3.8-27b":
    name: "Qwen3.8-27B (gufo, DFlash 2)"
    cmd: >-
      ${gufo} --served-model-name qwen3.8-27b
      --model ${env.MODELS_BASE}/qwen3.8-27b/Qwen3.8-27B-UD-Q4_K_XL.gguf
      --speculative dflash2
      --dflash-model ${env.MODELS_BASE}/qwen3.8-27b/Qwen3.8-27B-DFlash2-Q8_0.gguf
      ${qwen}
    checkEndpoint: /health
    capabilities:
      context: 262144

  # Qwen3.8-Flash-Next : cible UD-Q4_K_XL unsloth (--gufo-download flashnext ;
  # gufo refuse le fine-tune Signal AP-Q4_K_XL du parc, tenseur IQ4_NL), tête
  # MTP shared Q8_0 et mmproj BF16 du parc.
  "qwen3.8-flash-next":
    name: "Qwen3.8-Flash-Next (gufo, MTP)"
    cmd: >-
      ${gufo} --served-model-name qwen3.8-flash-next
      --model ${env.GUFO_DATA}/models/qwen3.8-flash-next/UD-Q4_K_XL/Qwen3.8-Flash-Next-UD-Q4_K_XL-00001-of-00004.gguf
      --speculative mtp
      --mtp-model ${env.MODELS_BASE}/qwen3.8-flash-next/MTP/mtp-Qwen3.8-Flash-Next-shared-Q8_0.gguf
      --mmproj ${env.MODELS_BASE}/qwen3.8-flash-next/mmproj-BF16.gguf
      ${qwen}
    checkEndpoint: /health
    capabilities:
      context: 262144

  # DeepSeek V4 Flash : seule quant acceptée par gufo, IQ2XXS d'antirez +
  # support DSpark (--gufo-download deepseek ; l'UD-IQ3_XXS du parc est
  # refusé). Plus légère que celle du service : un comptage raté à 26k tokens
  # le 24/09/2026, justesse à surveiller. 16/16 en boucle agentique pi.
  # Mesuré le 25/09/2026 : répond sans raisonnement par défaut (la section
  # du service raisonne, budget 6 144 tokens).
  "deepseek-v4-flash":
    name: "DeepSeek V4 Flash (gufo, IQ2XXS antirez, DSpark)"
    cmd: >-
      ${gufo} --served-model-name deepseek-v4-flash
      --model ${env.GUFO_DATA}/models/DeepSeek-V4-Flash-IQ2XXS-w2Q2K-AProjQ8-SExpQ8-OutQ8-chat-v2-imatrix-0731.gguf
      --speculative dspark
      --dspark-model ${env.GUFO_DATA}/models/DeepSeek-V4-Flash-DSpark-support-0731.gguf
      ${deepseek}
    checkEndpoint: /health
    capabilities:
      context: 262144

  # ── Audio et image : autres serveurs de gufo (voir docs/models/ de gufo) ──
  # Poids BF16 safetensors dans GUFO_DATA/models (--gufo-download tts, asr,
  # qwen-image). Routes relayées par llama-swap : /v1/audio/speech,
  # /v1/audio/voices, /v1/audio/transcriptions, /v1/images/generations et
  # /v1/images/edits. Le flux WebSocket /v1/audio/speech/stream de la synthèse
  # n'est pas une route de llama-swap : passer par /upstream/<modèle>/.

  # Qwen3-TTS 12Hz 1.7B, trois variantes (une seule chargée à la fois) :
  #   customvoice  voix intégrées au modèle (« voice »)
  #   voice-design voix décrite en texte (« instructions »)
  #   base         clonage : audio de référence et sa transcription par requête
  # Mesuré par gufo : 2,54 fois le temps réel, 201 ms avant le premier son.
  # Mesuré ici le 25/09/2026 : 2,5 fois le temps réel ; Flash-Next + voix +
  # transcription chargés ensemble = 110 Gio, 14 Gio libres.
  # GPU_MAX_HW_QUEUES=1 et ttl 60, voix ET transcription (gufo-org/gufo#272,
  # mesuré le 25/09/2026, gufo 98641a6) : au-delà de 8 files de calcul
  # matérielles, tous processus confondus, le GPU reste occupé à 100 % au repos
  # (~30 W contre ~4 W, 50 °C, ventilateurs jamais arrêtés). Chaque serveur
  # gufo en ouvre 4 à 5 par défaut (le LLM 5, la voix 4) : LLM + voix = 9.
  # GPU_MAX_HW_QUEUES=1 ramène la voix et la transcription à 2 (synthèse et
  # transcription aussi rapides : 2,23 à 2,28 s, 0,45 s), mais LLM + voix +
  # transcription font encore 9 : le ttl décharge celui des deux qui ne sert
  # plus. Rechargement en moins d'une seconde (332 à 1236 ms).
  "qwen3-tts-12hz-1.7b-customvoice":
    name: "Qwen3-TTS 1.7B CustomVoice (gufo)"
    cmd: >-
      gufo serve tts --host 127.0.0.1 --port ${PORT}
      --model ${env.GUFO_DATA}/models/Qwen3-TTS-12Hz-1.7B-CustomVoice
      --served-model-name qwen3-tts-12hz-1.7b-customvoice
    checkEndpoint: /health
    env:
      - "GPU_MAX_HW_QUEUES=1"
    ttl: 60

  "qwen3-tts-12hz-1.7b-voice-design":
    name: "Qwen3-TTS 1.7B VoiceDesign (gufo)"
    cmd: >-
      gufo serve tts --host 127.0.0.1 --port ${PORT}
      --model ${env.GUFO_DATA}/models/Qwen3-TTS-12Hz-1.7B-VoiceDesign
      --served-model-name qwen3-tts-12hz-1.7b-voice-design
    checkEndpoint: /health
    env:
      - "GPU_MAX_HW_QUEUES=1"
    ttl: 60

  "qwen3-tts-12hz-1.7b-base":
    name: "Qwen3-TTS 1.7B Base, clonage de voix (gufo)"
    cmd: >-
      gufo serve tts --host 127.0.0.1 --port ${PORT}
      --model ${env.GUFO_DATA}/models/Qwen3-TTS-12Hz-1.7B-Base
      --served-model-name qwen3-tts-12hz-1.7b-base
    checkEndpoint: /health
    env:
      - "GPU_MAX_HW_QUEUES=1"
    ttl: 60

  # Qwen3-ASR 1.7B : WAV jusqu'à 30 min, découpé en morceaux, décodage glouton.
  # Mesuré par gufo : 15,27 fois le temps réel. env et ttl : voir Qwen3-TTS.
  "qwen3-asr-1.7b":
    name: "Qwen3-ASR 1.7B (gufo)"
    cmd: >-
      gufo serve asr --host 127.0.0.1 --port ${PORT}
      --model ${env.GUFO_DATA}/models/Qwen3-ASR-1.7B
      --served-model-name qwen3-asr-1.7b
    checkEndpoint: /health
    env:
      - "GPU_MAX_HW_QUEUES=1"
    ttl: 60

  # Qwen-Image-2.1 : génération et édition (jusqu'à 10 références), sortie PNG
  # en base64, 1024² et 40 étapes par défaut. Licence Qwen Research, non
  # commerciale. Dans le groupe « gros » : une image décharge le LLM en cours
  # (et le LLM suivant décharge Qwen-Image). Temps de génération non publiés
  # par gufo ; mesuré ici le 25/09/2026 : 1024² en 40 étapes 90 s, 512² en
  # 20 étapes 16 s, retour à Flash-Next 38 s.
  # Seule variante servie depuis le 25/09/2026 : même pipeline, encodeur de
  # texte « abliterated » (catplusplus/Qwen21_Text_Encoder_Heretic, 54
  # tenseurs sur 749 : o_proj et down_proj des couches 9 à 35 du langage ;
  # vision, transformer et VAE identiques bit à bit). D'après sa fiche,
  # l'encodeur d'origine dévie le conditionnement sur les sujets « sensibles »
  # (vêtements ajoutés, poses figées). Mêmes temps ; images quasi identiques à
  # l'officiel sur 4 prompts à graine fixe, régression non mesurée au-delà
  # (texte dans l'image, prompts complexes, édition). Dossier monté par
  # download.sh qwen-image-heretic, qui lie le pipeline officiel.
  "Qwen-Image-2.1-heretic":
    name: "Qwen-Image-2.1, encodeur Heretic (gufo)"
    cmd: >-
      gufo serve image --host 127.0.0.1 --port ${PORT}
      --model ${env.GUFO_DATA}/models/Qwen-Image-2.1-heretic
      --served-model-name Qwen-Image-2.1-heretic
    checkEndpoint: /health

routing:
  router:
    use: group
    settings:
      groups:
        gros:
          # Un seul membre à la fois (swap), et il décharge tout groupe non
          # persistant.
          swap: true
          exclusive: true
          members:
            - "qwen3.8-27b"
            - "qwen3.8-flash-next"
            - "deepseek-v4-flash"
            - "Qwen-Image-2.1-heretic"
        voix:
          # Une variante à la fois ; ne décharge pas le gros modèle
          # (exclusive: false) et n'est pas déchargée par lui (persistent).
          swap: true
          exclusive: false
          persistent: true
          members:
            - "qwen3-tts-12hz-1.7b-customvoice"
            - "qwen3-tts-12hz-1.7b-voice-design"
            - "qwen3-tts-12hz-1.7b-base"
        transcription:
          swap: false
          exclusive: false
          persistent: true
          members:
            - "qwen3-asr-1.7b"

hooks:
  on_startup:
    # Modèle chargé au démarrage du conteneur : GUFO_PRECHARGE du .env
    # (./setup-llm.sh --gufo [modèle], qwen3.8-flash-next par défaut).
    preload:
      - "${env.GUFO_PRECHARGE}"
